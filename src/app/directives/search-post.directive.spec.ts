import { SearchPostDirective } from './search-post.directive';

describe('SearchPostDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchPostDirective();
    expect(directive).toBeTruthy();
  });
});
