import { PostdateformatterPipe } from './postdateformatter.pipe';

describe('PostdateformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PostdateformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
